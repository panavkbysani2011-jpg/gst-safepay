export function ComplianceDisclaimer() {
  return (
    <div className="rounded-lg border border-amber-800/50 bg-amber-950/30 px-4 py-3 text-sm text-amber-200/90">
      <span className="font-semibold">Prototype — not tax advice.</span> The
      tax-rule parameters (RBI bank rate, MSMED interest multiplier, deduction
      rate) are placeholder defaults and must be verified by a chartered
      accountant before you rely on these figures for real payments.
    </div>
  );
}
