# Copy to .env and fill in. .env is gitignored (machine-local, per the continuity design).
# Dev: local SQLite file. Production: swap to a cloud Postgres connection string (Supabase/Neon).
DATABASE_URL="file:./dev.db"
